window.APP_CONFIG = {
  // Cambia esta URL cuando publiques el backend.
  // Ejemplo: https://api.tu-dominio.com
  apiBaseUrl: "http://localhost:3000", // Reemplazar por https://TU-API antes del release
};
