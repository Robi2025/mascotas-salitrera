FROM node:22-bookworm-slim
WORKDIR /app
COPY package*.json ./
RUN npm install --omit=dev
COPY src ./src
RUN mkdir -p /app/data
ENV NODE_ENV=production PORT=3000 DB_PATH=/app/data/mascotas.db
EXPOSE 3000
CMD ["npm", "start"]
